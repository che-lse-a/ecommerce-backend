# Backend (Express + MongoDB)
## Setup
1. Copy `.env.example` to `.env` and fill `MONGO_URI` and `JWT_SECRET`.
2. `npm install`
3. `npm run dev` (requires nodemon) or `npm start`
## Notes
- This is a minimal demo backend for development and learning.
- Protect admin routes and validate inputs for production.
